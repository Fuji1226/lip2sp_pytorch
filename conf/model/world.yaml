# @package _group_

name : world

#################################
# data_info
#################################
# データを置いているディレクトリまでのパス
train_path : "/users/minami/dataset/train"
test_path : "/users/minami/dataset/test"

# 学習したモデルのパラメータを保存するディレクトリまでのパス
train_save_path : "/Users/minami/Documents/python/lip2sp_pytorch/mlruns/2/34f5be03d69f4b6a81676b9421fce781/artifacts/model_state_dict"

# generate.pyを実行したときのデータを保存するディレクトリまでのパス
generate_save_path : "/users/minami/result/generate"

# video parameter
fps : 50
video_channels : 3

# audio parameter
sampling_rate : 16000
n_fft : 640
hop_length : 160
win_length : 640
f_min : 0
f_max : 7600
n_mel_channels : 80

# input grayscale.（グレースケールかRGBか）
gray : False

# input first and second derivative.（動的特徴量を使うかどうか）
delta : True

# frame period
frame_period : 10

# acoutic feature frames in training
length : 300     # Unet-discriminatorを使用するときは402か202で！

# reduction factor
reduction_factor : 2

# "world" or "mspec"（音響特徴量の選択）
feature_type : "world"

#################################
# model_info
#################################
# in_channels
in_channels : 5

# ResNet3D parameter
res_layers : 5

# glu parameter
# glu_inner_channels : 256
glu_layers : 4

# transformer parameter
n_layers : 6
d_model : 128
n_head : 2

# 使用するencoder（"transformer" or "conformer"）
which_encoder : "transformer"

# 使用するdecoder（"transformer" or "glu"）
which_decoder : "glu"

# discriminator（"unet" or "jcu"）
# 使わない場合は何も書かない（Noneもいらないです）
which_d : 

# out_channels
out_channels : 29  # 音響特徴量から計算します

# Prenet & Postnet parameter
pre_in_channels : 58  # 音響特徴量から計算します
pre_inner_channels : 32
post_inner_channels : 512