---
program: train_wandb.py
method: bayes
metric:
    goal: minimize
    name: val_iter_loss
parameters:
    model.n_layers : 
      values : [1, 2]

    model.d_model :
      values : [32, 64]

    model.n_head :
      values : [1, 2]

command:
    - ${env}
    - ${interpreter}
    - ${program}
    - ${args_no_hyphens}